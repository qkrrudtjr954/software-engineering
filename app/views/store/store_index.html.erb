<p>
  <h1>상품을 등록해주세요!</h1>
</p>

<div class="jumbotron">
  <div>
    <form action="/store/create" method="post" enctype="multipart/form-data">
      <div class="form-group">
        <div class="input-group">
          <input type="file" name="pic" accept="image/*">
        </div>
        <div class="input-group">
          <span class="input-group-addon">품명</span>
          <input name="state" type="text" placeholder="상품명을 정확히 기재해 주십시오."class="form-control">
        </div>
        <div class="input-group">
          <span class="input-group-addon">가격</span>
          <input name="cost" type="text" class="form-control" placeholder="정확한 가격을 입력해주시기 바랍니다.">
          <span class="input-group-addon">원</span>
        </div>
        <div class="input-group">
          <span class="input-group-addon">설명</span>
          <textarea name="content" class="form-control"  row="1" placeholder="Share what you want to know."></textarea>
        </div>
        <input type="hidden" name="user_name" value="<%=current_user.name%>">
        <button type="submit" class="btn btn-default">Submit</button>
      </div>
    </form>
  </div>
</div>

<div class="row marketing">
  <table class="qna_index_table"  width="95%">
    <tr height="30">
      <td><kbd></kbd></td>
      <td width="55%" style="word-break:break-all">상품명</td>
      <td width="15%" style="word-break:break-all">가격</td>
      <td class="text-center">작성시간</td>
    </tr>
    <% @store.each do |p| %>
    <tr height="30">
      <td><kbd><%= p.id %></kbd></td>
      <td width="50%" style="word-break:break-all"><a href="/store/store_detail/<%=p.id%>">&nbsp;<%=p.state %>&nbsp;</a></td>
      <% if p.cost > 10000 %>
        <td width="20%" style="word-break:break-all"><span style="color:darkorange"><%=p.cost/10000%></span><small>만원</small></td>
      <%else%>
        <td width="20%" style="word-break:break-all"><span style="color:darkorange"><%=p.cost%></span><small>원</small></td>
      <%end%>
      <td class="text-right"><small style="color:#737373"><%= time_ago_in_words(p.created_at) %> ago</small></td>
      <td width="15%">
        <a href="/store/store_destroy/<%=p.id%>">삭제</a>
      </td>
    </tr>
    <% end %>
  </table>
</div>
